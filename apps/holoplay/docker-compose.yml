name: holoplay
services:
  holoplay:
    image: spout8301/holoplay:1.12.3
    ports:
    - target: 3000
      published: '3000'
      protocol: tcp
    privileged: true
    restart: unless-stopped
    container_name: holoplay
x-casaos:
  main: holoplay
  icon: https://cdn.jsdelivr.net/gh/jerrykuku/ZimaOS-AppStore@gh-pages/apps/holoplay/icon.svg
  title:
    en_US: HoloPlay
  index: /
  port_map: '3000'
